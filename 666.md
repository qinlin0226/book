# Java 后端开发规范手册
## 1. 基础编码规范
### 1.1 命名与风格
| 类型 | 规范 | 示例 | 备注 |
| --- | --- | --- | --- |
| 类名 | 大驼峰 (UpperCamelCase) | UserService, OrderController | 名词为主 |
| 方法名 | 小驼峰 (lowerCamelCase) | getUserById(), processPayment() | 动词开头 |
| 常量名 | 全大写 + 下划线 | MAX_RETRY_COUNT, DEFAULT_TTL | static final |
| 布尔变量 | 避免否定词 | isEnabled (✔️) / isNotFound (❌) | 防止逻辑歧义 |
| 包名 | 全小写 | com.company.module.service | 域名倒序 |

**代码格式约束**
- 缩进：4 个空格 (IDE 设置 Use space instead of tab)
- 行宽：单行不超过 120 字符
- 大括号：K&R 风格 (左括号不换行)
- 导入：禁止使用 `import java.util.*`，除非同一包下引用超过 5 个类

### 1.2 Lombok 最佳实践
Lombok 能减少样板代码，但滥用会导致调试困难或序列化问题。
- **Entity (PO) 类**
  - 禁止直接使用 `@Data` (隐含全字段 equals/hashCode，易导致 N+1 或 HashSet 问题)
  - 推荐：显式组合注解
- **DTO / VO 类**
  - 可以使用 `@Data`，建议配合 `@Builder` 使用
  - VO (View Object) 建议只生成 Getter，保持不可变性
- **依赖注入**
  - 推荐：`@RequiredArgsConstructor` (配合 final 字段)，替代 `@Autowired` 字段注入

### 1.3 异常处理
- 禁止：捕获 Exception 后不做处理 (Swallow Exception)，或打印 `e.printStackTrace()`
- 规范：使用统一异常处理机制，区分业务异常与系统异常

---

## 2. 架构与分层设计
### 2.1 分层架构 (Clean Architecture 变体)
严格遵循依赖倒置原则，禁止跨层调用。
- Controller：仅负责参数校验、请求转发、结果封装，禁止包含业务逻辑
- Service：事务控制、核心业务逻辑、领域模型编排
- Repository/Mapper：仅负责 CRUD，禁止包含业务判断

### 2.2 对象转换规范 (DO/DTO/VO)
禁止在不同层级间直接传递对象 (如直接返回 Entity 给前端)。

| 对象类型 | 全称 | 用途 | 规范 |
| --- | --- | --- | --- |
| DO/PO | Domain Object | 数据库映射 | 与表结构一致，含 `@TableField` 等注解 |
| DTO | Data Transfer Object | 服务间/入参 | 校验注解 (`@NotNull`) 放在此处 |
| VO | View Object | 前端展示 | 脱敏处理，按需组装字段 |

---

## 3. 微服务与云原生
### 3.1 微服务拆分与交互
- 服务粒度：基于业务领域驱动设计 (DDD) 拆分，单一职责
- 通信协议：内部调用使用 OpenFeign (HTTP/REST)，高性能场景可考虑 Dubbo (RPC)
- 幂等性：写操作接口 (POST/PUT/DELETE) 必须设计幂等性 (Token 机制或数据库唯一索引)

### 3.2 OpenFeign 规范
- 超时配置：必须配置连接和读取超时，防止线程池耗尽
- 降级处理：必须实现 FallbackFactory，记录错误日志并返回兜底数据
- 上下文透传：必须透传 TraceId、Auth Token 等请求头

### 3.3 网关 (Gateway)
- 职责：路由转发、鉴权 (黑白名单)、限流 (Sentinel/Redis)、日志切面
- 跨域：统一在网关层处理 CORS，下游服务无需配置

### 3.4 分布式事务 (Seata)
- 模式选择：优先使用 AT 模式 (自动补偿)，复杂场景使用 TCC
- 规范：`@GlobalTransactional` 仅标注在事务发起方 (通常是 Controller 调用的第一个 Service 方法)

---

## 4. 持久层 (MyBatis-Plus)
### 4.1 SQL 与索引
- 禁止：`SELECT *`，必须显式指定字段
- 索引：WHERE、ORDER BY、GROUP BY 涉及的字段必须评估索引
- 分页：禁止内存分页。大数据量深分页 (如 LIMIT 100000, 10) 需优化为游标分页或 ES 查询

### 4.2 MyBatis-Plus 使用
- Wrapper：优先使用 LambdaQueryWrapper (类型安全)，禁止使用 QueryWrapper (硬编码字符串)
- 批量操作：必须使用 saveBatch / updateBatchById，禁止在 for 循环中调用单条插入
- 逻辑删除：全局配置逻辑删除插件，禁止物理删除业务数据

### 4.3 事务管理
- 范围：事务控制在 Service 层
- 只读：查询方法添加 `@Transactional(readOnly = true)`，提升性能
- 回滚：默认仅回滚 RuntimeException，若需回滚受检异常，需配置 `rollbackFor = Exception.class`

---

## 5. 安全与性能
### 5.1 安全规范
- 输入校验：使用 `@Validated` + JSR-380 注解 (`@NotBlank`, `@Size`)
- 防注入：禁止使用 `${}` 拼接 SQL，必须使用 `#{}` 预编译
- 敏感数据：密码必须 BCrypt 加密；日志中必须脱敏手机号、身份证
- 配置安全：数据库密码、密钥等禁止硬编码，使用 Nacos Config + 环境变量注入

### 5.2 性能与并发
- 集合初始化：`new ArrayList<>(size)` 指定初始容量，避免扩容开销
- 线程池：禁止使用 Executors 创建线程池 (防止 OOM)，必须使用 ThreadPoolTaskExecutor 并显式配置队列大小和拒绝策略
- 缓存：使用 Redis 缓存热点数据，设置随机 TTL 防止缓存雪崩

---

## 6. 工程化与 DevOps
### 6.1 Git 提交规范
采用 Conventional Commits 规范，格式：`type(scope): subject`
- feat: 新功能
- fix: 修补 bug
- docs: 文档变更
- style: 格式调整 (不影响代码运行)
- refactor: 重构
- test: 测试用例
- chore: 构建过程或辅助工具变动

示例：`feat(user): add login interface`

### 6.2 Docker 构建
使用多阶段构建以减小镜像体积。

---

## 附录：AI 代码审查清单
提交 AI 生成的代码前，请自检以下项目：
1. SQL 安全：是否存在 `${}` 拼接风险？
2. 空指针：Optional 使用是否得当？List 判空了吗？
3. 事务：`@Transactional` 是否加在 public 方法上？同类内部调用事务是否失效？
4. Lombok：Entity 是否误用了 `@Data`？
5. 资源泄露：InputStream、Connection 是否使用了 try-with-resources？
6. 日志：是否使用了 `{}` 占位符？是否打印了敏感信息？