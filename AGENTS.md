# 图书系统 Agent 指令

这是一个历史记录和全局规范文件，你每次开始任务时都应该仔细阅读此文件内容并遵循

## 历史记录添加

用户每次提出问题后输出后，你都要以如下格式保存在"历史记录.md"文件中，如果没有则创建，不要覆盖之前记录

```
用户的问题：XXXXX（用户输入的命令）
完成结果：XXXXX(完成/未完成)
```
## 全局规范

每次检查代码时在代码上面添加中文注释，注释种类具体如下，在不同场景下使用合适的注释

```
	/**
	 * 主键ID
	 */
	private Long id;

/**
 * XXXX(什么类/什么接口/做了什么/什么功能)
 *
 * @XXXX
 */
 
 /**
	 * XXXX(什么功能)
	 *
	 * @param XXXX  (授权参数)
	 * @return XXXX (返回值)
	 */
	 
// XXXX(具体功能描述)
```

前后端启动由用户手动操作，你只负责代码优化，由用户进行测试，生成一个测试.md文件告诉用户如何测试