# 图书系统 Agent 指令

这是一个历史记录和全局规范文件，你每次开始任务时都应该仔细阅读此文件内容并遵循

## 历史记录添加

用户每次提出问题后输出后，你都要以如下格式保存在"历史记录.md"文件中，如果没有则创建，不要覆盖之前记录

```
用户的问题：XXXXX（用户输入的命令）
完成结果：XXXXX(完成/未完成)
```
## 全局规范

每次检查代码时在代码上面添加中文注释，注释种类具体如下，在不同场景下使用合适的注释

```
	/**
	 * 主键ID
	 */
	private Long id;

/**
 * XXXX(什么类/什么接口/做了什么/什么功能)
 *
 * @XXXX
 */
 
 /**
	 * XXXX(什么功能)
	 *
	 * @param XXXX  (授权参数)
	 * @return XXXX (返回值)
	 */
	 
// XXXX(具体功能描述)
```

用户提问后立刻查看端口，如果端口已经启动则回答问题

没有则启动前后端代码方便用户使用前端页面查看具体效果

用户使用了虚拟机，服务地址为192.168.101.129，默认所有远端服务正常，直接启动前后端程序即可

后端使用mvn命令启动，前端使用npm命令启动

如果用户输入问题使用的是中文，那你输出也应该使用中文，这样可以优化用户体验