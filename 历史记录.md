用户的问题：关闭8080-8084端口
完成结果：完成

用户的问题：@AGENTS.md 目前前端接口与样式文件掺杂在一起，不符合高内聚低耦合的开发思想，优化前端代码，实现高内聚低耦合，将接口都放在一起
完成结果：完成

用户的问题：好
完成结果：完成

用户的问题：继续
完成结果：完成
用户的问题：[AGENTS.md](AGENTS.md) 前端页面优化，左侧格子里所有上架图书图片自动向下循环滚动，右侧格子里所有上架图书图片也自动向下循环滚动，但是两侧格子里循环的图书图片不一样
完成结果：完成
用户的问题：修改主推荐为所有上架图书随机展示，修改左右侧为1列循环
完成结果：完成
用户的问题：修改左右侧展示底部与主推荐齐平
完成结果：完成
用户的问题：把左右侧的循环速度和宽度改为一致，
完成结果：完成
用户的问题：删除这个展示格
完成结果：完成
用户的问题：把在读书单改为追更数，推荐命中改为阅读数
完成结果：完成
用户的问题：把阅读身份改为真实用户身份，根据数据库role字段角色展示
完成结果：完成
用户的问题：把加入在读改为加入书架，固定大小大小与直接编辑一致并与作者明保持良好的距离，删除右边的文字
完成结果：完成
用户的问题：PLEASE IMPLEMENT THIS PLAN:
## 前端架构优化计划：先拆 `App.jsx`

### Summary
保留现在的“单页 + 顶部 Tab 切换”产品形态，不引入路由库，也不引入 Redux/Zustand。  
这次优化的目标是把 [App.jsx](D:/dddjava/ai-opencode/book/book-frontend/src/App.jsx) 从“全能入口组件”拆成 **壳层 + 会话层 + 业务数据层 + 页面容器层**，让后续功能继续迭代时不再往 `App.jsx` 里堆状态和动作。

默认选择：
- 不改后端接口
- 不改当前页面行为
- 不新增复杂状态管理库
- 以“降低耦合、减少 prop drilling、把业务规则移出页面”为主

### Key Changes
#### 1. 把 `App.jsx` 降级成纯壳层
`App.jsx` 只保留：
- `activeTab`
- 全局布局壳（左右侧、顶部导航、模态框挂载位）
- 页面切换逻辑
- Provider 装配

从 `App.jsx` 移出：
- 书籍/推荐/阅读记录/用户资料/管理用户等服务端数据状态
- 表单状态
- 业务动作函数
- 业务派生逻辑（随机推荐、左右侧拆分、角色标题、阅读状态文案等）

#### 2. 新增两层 Hook/Context，替代入口组件集中编排
新增两个核心层：

**A. 会话层**
- `useSessionController` 或 `SessionProvider`
- 负责：持久化登录态、登录/注册、退出、`isAdmin`、`sessionToken`
- `usePersistedSession` 保留，但只作为底层能力，不再直接被页面使用

**B. 业务数据层**
- `useLibraryWorkspace` 或 `LibraryProvider`
- 统一负责：
  - 公共图书列表
  - 推荐数据
  - 用户资料
  - 阅读记录
  - 管理员图书列表
  - 管理用户列表
  - 对应 loading 状态
  - 刷新/失效逻辑
- 所有“改完一个资源后需要联动刷新哪些数据”的规则集中到这里，不再分散在页面动作函数里

默认实现选择：
- 使用 React Context + 自定义 Hook
- 不使用第三方全局状态库
- 数据刷新仍基于现有 API Promise 组合，不引入 React Query

#### 3. 页面视图改成“容器 + 展示”结构
保留现有 `HomeView / ProfileView / AdminView` 作为展示组件，但新增对应页面容器：
- `HomePageContainer`
- `ProfilePageContainer`
- `AdminPageContainer`

容器层负责：
- 从 Context/Hooks 取数据
- 组装页面专属 props
- 管理本页面本地 UI 状态（如搜索词、轮播索引、表单草稿）

展示层只负责：
- 渲染
- 触发回调
- 不直接知道 API 和跨页面刷新规则

#### 4. 把业务规则抽成 selectors / domain helpers
把这些逻辑从页面 JSX 中抽走，集中到 `utils` 或新建 `domain` 目录：
- `role -> 身份标题`
- `readStatus -> 标签/className`
- 上架书单筛选
- 首页随机推荐
- 左右侧封面流拆分
- 用户中心统计卡数值
- 首页当前书籍对应阅读记录查找

目标：
- 页面组件不再写大段 `filter/map/role===1 ? ... : ...`
- 业务规则能单独测试
- 后续文案或规则变化只改一处

#### 5. 清理当前已经失效的状态与职责
在重构过程中顺手清掉当前无效或位置不对的部分：
- 删除 `message` 这类已无展示承载的状态写入
- 将页面局部表单状态从全局入口下放到对应页面容器
- 将首页左右侧高度同步逻辑封装为单独 Hook（例如 `useSideMarqueeHeightSync`），避免入口组件继续膨胀

### Interfaces / Public Changes
不改后端接口，不改现有 API 路径。  
前端内部新增的稳定接口建议为：

- `useSession()`  
  返回：`session`, `isAdmin`, `login`, `register`, `logout`
- `useLibraryWorkspace()`  
  返回分组对象：
  - `home`
  - `profile`
  - `admin`
  - `actions`
- `selectors/*` 或 `domain/*`
  输出纯函数，不依赖 React

对外行为保持一致：
- 仍然是顶部 Tab 切换
- 仍然是现有三大页面
- 管理员/普通用户行为边界不变

### Test Plan
实现后至少验证这些场景：

1. **构建与基础可运行**
- `npm run build` 通过
- 首页、用户中心、管理台切换正常

2. **会话相关**
- 注册/登录后 session 正常持久化
- 退出登录后页面状态正确回退
- `role` 为管理员时显示管理员入口，否则不显示

3. **首页**
- 上架图书正常加载
- 搜索、推荐随机展示、上下切换正常
- 加入书架/切换阅读状态后，首页和用户中心数据同步刷新
- 左右侧封面流仍正常渲染

4. **用户中心**
- 身份标题与数据库 `role` 一致
- 追更数/已读完/阅读数统计正确
- 修改资料后资料区即时更新
- 阅读记录删除/切换状态正常

5. **管理台**
- 图书新增/编辑/上下架/删除后首页和管理台同步刷新
- 用户管理列表读取、编辑、删除正常

### Assumptions
- 继续使用当前 React 18 + Vite 技术栈
- 当前应用仍以“单页内 Tab 切换”为主，不引入 `react-router`
- 当前 API 返回结构稳定，不需要同时做接口协议重构
- 本轮目标是“架构解耦”，不是视觉重做，也不是引入完整测试框架迁移
- 通知条已被移除，因此 `message` 默认视为可清理状态；如后续要恢复反馈，再单独引入轻量 toast 方案
完成结果：完成
