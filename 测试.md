# 后端模块测试说明

## 测试前说明

- 本次仅完成后端代码优化与重构，前后端启动由你手动执行。
- 我已完成一次 `mvn -q -DskipTests compile` 编译校验，通过目录为 `D:\dddjava\ai-opencode\book\book-system`。
- 建议按模块顺序联调：`book-gateway`、`book-user`、`book-book`、`book-record`、`book-recommend`。

## 1. 用户模块 `book-user`

### 注册

- 接口：`POST /api/user/register`
- 请求体示例：

```json
{
  "username": "reader01",
  "password": "123456",
  "nickname": "读者01"
}
```

- 预期结果：
  - 返回 `token`、`userId`、`nickname`、`role`
  - 相同用户名重复注册时，返回业务错误

### 登录

- 接口：`POST /api/user/login`
- 请求体示例：

```json
{
  "username": "reader01",
  "password": "123456"
}
```

- 预期结果：
  - 登录成功返回统一响应结构
  - 错误密码返回友好错误信息

### 个人资料

- 接口：`GET /api/user/profile`
- 请求头：`Authorization: Bearer {token}`
- 预期结果：
  - 不再直接返回数据库实体中的敏感字段

### 管理员用户管理

- 接口：
  - `GET /api/user/admin/list`
  - `POST /api/user/admin`
  - `PUT /api/user/admin/{id}`
  - `DELETE /api/user/admin/{id}`
- 预期结果：
  - 普通用户访问返回无权限
  - 管理员可以正常增删改查用户

## 2. 图书模块 `book-book`

### 图书列表与详情

- 接口：
  - `GET /api/book`
  - `GET /api/book/{id}`
  - `GET /api/book/random`
- 预期结果：
  - 游客可访问公开图书接口
  - 返回值为统一图书响应对象
  - 分类、关键字、分页参数都能正常生效

### 图书管理

- 接口：
  - `POST /api/book`
  - `PUT /api/book/{id}`
  - `PUT /api/book/{id}/status`
  - `DELETE /api/book/{id}`
- 预期结果：
  - 只有管理员可操作
  - 状态字段非法时返回参数错误
  - 删除后图书不再出现在公开列表中

## 3. 阅读记录模块 `book-record`

### 新增阅读记录

- 接口：`POST /api/record`
- 请求体示例：

```json
{
  "bookId": 1
}
```

- 预期结果：
  - 只能为已上架图书创建记录
  - 重复添加同一本书时返回业务冲突错误

### 查询与维护阅读记录

- 接口：
  - `GET /api/record`
  - `GET /api/record/{id}`
  - `GET /api/record/recently-read`
  - `PUT /api/record/{id}/status`
  - `DELETE /api/record/{id}`
- 预期结果：
  - 只能访问自己的阅读记录
  - 下架图书对应记录会被过滤
  - 阅读状态更新后最近阅读列表正常变化

## 4. 推荐模块 `book-recommend`

### 综合推荐

- 接口：`GET /api/recommend`
- 预期结果：
  - 能同时返回推荐图书和最近阅读记录
  - 返回结构统一且不直接暴露实体对象

### 个性化推荐

- 接口：`GET /api/recommend/personalized?category=文学&limit=10`
- 预期结果：
  - 优先返回指定分类图书
  - 若分类结果不足，会自动回补随机图书，不会直接返回空列表

## 5. 网关模块 `book-gateway`

- 验证项：
  - 未登录访问 `/api/user/login`、`/api/user/register` 可以放行
  - 未登录访问公开图书 GET 接口可以放行
  - 未登录访问受保护接口返回 `401`
  - 登录后访问受保护接口时，网关会透传 `X-User-Id`、`X-Username`、`X-User-Role`

## 6. 回归关注点

- 所有接口异常是否都返回统一 `Result` 结构
- DTO 参数校验是否生效
- 管理员权限控制是否生效
- 阅读记录模块的 Feign 调用是否能正常获取图书详情
- 推荐模块在图书服务或记录服务异常时是否能看到降级后的错误提示
